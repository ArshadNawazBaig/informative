import LoginWrapper from '@/sections/Login';
import React from 'react';

export const metadata = {
  title: 'Login - Informative',
  description:
    'Login to your Informative account. Access exclusive content, manage your profile, and stay connected with us.',
};

const LoginPage = () => {
  return <LoginWrapper />;
};

export default LoginPage;
